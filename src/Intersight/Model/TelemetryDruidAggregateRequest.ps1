#
# Cisco Intersight
# Cisco Intersight is a management platform delivered as a service with embedded analytics for your Cisco and 3rd party IT infrastructure. This platform offers an intelligent level of management that enables IT organizations to analyze, simplify, and automate their environments in more advanced ways than the prior generations of tools. Cisco Intersight provides an integrated and intuitive management experience for resources in the traditional data center as well as at the edge. With flexible deployment options to address complex security needs, getting started with Intersight is quick and easy. Cisco Intersight has deep integration with Cisco UCS and HyperFlex systems allowing for remote deployment, configuration, and ongoing maintenance. The model-based deployment works for a single system in a remote location or hundreds of systems in a data center and enables rapid, standardized configuration and deployment. It also streamlines maintaining those systems whether you are working with small or very large configurations. The Intersight OpenAPI document defines the complete set of properties that are returned in the HTTP response. From that perspective, a client can expect that no additional properties are returned, unless these properties are explicitly defined in the OpenAPI document. However, when a client uses an older version of the Intersight OpenAPI document, the server may send additional properties because the software is more recent than the client. In that case, the client may receive properties that it does not know about. Some generated SDKs perform a strict validation of the HTTP response body against the OpenAPI document. This document was created on 2020-07-16T08:45:19Z.
# Version: 1.0.9-2020
# Contact: intersight@cisco.com
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

Exposes a REST endpoint for performing queries against Druid time series data. View Telemetry allows [POST of a Druid query](http://druid.io/docs/latest/querying/querying). Manage Telemetry allows [READ of broker status](http://druid.io/docs/latest/operations/api-reference.html#broker).

.PARAMETER Json

JSON object

.OUTPUTS

TelemetryDruidAggregateRequest<PSCustomObject>
#>
function ConvertFrom-IntersightJsonToTelemetryDruidAggregateRequest {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        $JsonData = ConvertFrom-Json -InputObject $Json
        # check if the discriminator value is 'dataSourceMetadata'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "dataSourceMetadata") {
            # try to match TelemetryDruidDataSourceMetadataRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidDataSourceMetadataRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidDataSourceMetadataRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidDataSourceMetadataRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (dataSourceMetadata). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'groupBy'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "groupBy") {
            # try to match TelemetryDruidGroupByRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidGroupByRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidGroupByRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidGroupByRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (groupBy). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'scan'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "scan") {
            # try to match TelemetryDruidScanRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidScanRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidScanRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidScanRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (scan). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'segmentMetadata'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "segmentMetadata") {
            # try to match TelemetryDruidSegmentMetadataRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidSegmentMetadataRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidSegmentMetadataRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidSegmentMetadataRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (segmentMetadata). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidDataSourceMetadataRequest'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "telemetry.DruidDataSourceMetadataRequest") {
            # try to match TelemetryDruidDataSourceMetadataRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidDataSourceMetadataRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidDataSourceMetadataRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidDataSourceMetadataRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (telemetry.DruidDataSourceMetadataRequest). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidGroupByRequest'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "telemetry.DruidGroupByRequest") {
            # try to match TelemetryDruidGroupByRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidGroupByRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidGroupByRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidGroupByRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (telemetry.DruidGroupByRequest). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidScanRequest'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "telemetry.DruidScanRequest") {
            # try to match TelemetryDruidScanRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidScanRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidScanRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidScanRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (telemetry.DruidScanRequest). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidSegmentMetadataRequest'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "telemetry.DruidSegmentMetadataRequest") {
            # try to match TelemetryDruidSegmentMetadataRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidSegmentMetadataRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidSegmentMetadataRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidSegmentMetadataRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (telemetry.DruidSegmentMetadataRequest). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidTimeBoundaryRequest'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "telemetry.DruidTimeBoundaryRequest") {
            # try to match TelemetryDruidTimeBoundaryRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTimeBoundaryRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidTimeBoundaryRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidTimeBoundaryRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (telemetry.DruidTimeBoundaryRequest). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidTimeSeriesRequest'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "telemetry.DruidTimeSeriesRequest") {
            # try to match TelemetryDruidTimeSeriesRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTimeSeriesRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidTimeSeriesRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidTimeSeriesRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (telemetry.DruidTimeSeriesRequest). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidTopNRequest'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "telemetry.DruidTopNRequest") {
            # try to match TelemetryDruidTopNRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTopNRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidTopNRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidTopNRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (telemetry.DruidTopNRequest). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'timeBoundary'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "timeBoundary") {
            # try to match TelemetryDruidTimeBoundaryRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTimeBoundaryRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidTimeBoundaryRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidTimeBoundaryRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (timeBoundary). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'timeseries'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "timeseries") {
            # try to match TelemetryDruidTimeSeriesRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTimeSeriesRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidTimeSeriesRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidTimeSeriesRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (timeseries). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'topN'
        if ($JsonData.PSobject.Properties["queryType"].value -eq "topN") {
            # try to match TelemetryDruidTopNRequest defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTopNRequest $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidTopNRequest"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidTopNRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest) using the discriminator lookup (topN). Proceeding with the typical oneOf type matching."
            }
        }

        # try to match TelemetryDruidDataSourceMetadataRequest defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidDataSourceMetadataRequest $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidDataSourceMetadataRequest"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidDataSourceMetadataRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest). Proceeding to the next one if any."
        }

        # try to match TelemetryDruidGroupByRequest defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidGroupByRequest $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidGroupByRequest"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidGroupByRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest). Proceeding to the next one if any."
        }

        # try to match TelemetryDruidScanRequest defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidScanRequest $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidScanRequest"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidScanRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest). Proceeding to the next one if any."
        }

        # try to match TelemetryDruidSegmentMetadataRequest defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidSegmentMetadataRequest $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidSegmentMetadataRequest"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidSegmentMetadataRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest). Proceeding to the next one if any."
        }

        # try to match TelemetryDruidTimeBoundaryRequest defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTimeBoundaryRequest $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidTimeBoundaryRequest"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidTimeBoundaryRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest). Proceeding to the next one if any."
        }

        # try to match TelemetryDruidTimeSeriesRequest defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTimeSeriesRequest $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidTimeSeriesRequest"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidTimeSeriesRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest). Proceeding to the next one if any."
        }

        # try to match TelemetryDruidTopNRequest defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTopNRequest $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidTopNRequest"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidTopNRequest' defined in oneOf (IntersightTelemetryDruidAggregateRequest). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([TelemetryDruidDataSourceMetadataRequest, TelemetryDruidGroupByRequest, TelemetryDruidScanRequest, TelemetryDruidSegmentMetadataRequest, TelemetryDruidTimeBoundaryRequest, TelemetryDruidTimeSeriesRequest, TelemetryDruidTopNRequest]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("TelemetryDruidDataSourceMetadataRequest", "TelemetryDruidGroupByRequest", "TelemetryDruidScanRequest", "TelemetryDruidSegmentMetadataRequest", "TelemetryDruidTimeBoundaryRequest", "TelemetryDruidTimeSeriesRequest", "TelemetryDruidTopNRequest")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([TelemetryDruidDataSourceMetadataRequest, TelemetryDruidGroupByRequest, TelemetryDruidScanRequest, TelemetryDruidSegmentMetadataRequest, TelemetryDruidTimeBoundaryRequest, TelemetryDruidTimeSeriesRequest, TelemetryDruidTopNRequest]). JSON Payload: $($Json)"
        }
    }
}


