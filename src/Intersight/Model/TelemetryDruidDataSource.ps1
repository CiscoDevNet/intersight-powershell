#
# Cisco Intersight
# Cisco Intersight is a management platform delivered as a service with embedded analytics for your Cisco and 3rd party IT infrastructure. This platform offers an intelligent level of management that enables IT organizations to analyze, simplify, and automate their environments in more advanced ways than the prior generations of tools. Cisco Intersight provides an integrated and intuitive management experience for resources in the traditional data center as well as at the edge. With flexible deployment options to address complex security needs, getting started with Intersight is quick and easy. Cisco Intersight has deep integration with Cisco UCS and HyperFlex systems allowing for remote deployment, configuration, and ongoing maintenance. The model-based deployment works for a single system in a remote location or hundreds of systems in a data center and enables rapid, standardized configuration and deployment. It also streamlines maintaining those systems whether you are working with small or very large configurations. The Intersight OpenAPI document defines the complete set of properties that are returned in the HTTP response. From that perspective, a client can expect that no additional properties are returned, unless these properties are explicitly defined in the OpenAPI document. However, when a client uses an older version of the Intersight OpenAPI document, the server may send additional properties because the software is more recent than the client. In that case, the client may receive properties that it does not know about. Some generated SDKs perform a strict validation of the HTTP response body against the OpenAPI document. This document was created on 2020-12-08T20:53:20Z.
# Version: 1.0.9-2908
# Contact: intersight@cisco.com
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

A data source is the Apache Druid equivalent of a database table. However, a query can also masquerade as a data source, providing subquery-like functionality. Query data sources are currently supported only by GroupBy queries.

.PARAMETER Json

JSON object

.OUTPUTS

TelemetryDruidDataSource<PSCustomObject>
#>
function ConvertFrom-IntersightJsonToTelemetryDruidDataSource {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        $JsonData = ConvertFrom-Json -InputObject $Json
        # check if the discriminator value is 'join'
        if ($JsonData.PSobject.Properties["type"].value -eq "join") {
            # try to match TelemetryDruidJoinDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidJoinDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidJoinDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidJoinDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (join). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'lookup'
        if ($JsonData.PSobject.Properties["type"].value -eq "lookup") {
            # try to match TelemetryDruidLookupDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidLookupDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidLookupDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidLookupDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (lookup). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'query'
        if ($JsonData.PSobject.Properties["type"].value -eq "query") {
            # try to match TelemetryDruidQueryDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidQueryDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidQueryDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidQueryDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (query). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'scan'
        if ($JsonData.PSobject.Properties["type"].value -eq "scan") {
            # try to match TelemetryDruidInlineDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidInlineDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidInlineDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidInlineDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (scan). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'table'
        if ($JsonData.PSobject.Properties["type"].value -eq "table") {
            # try to match TelemetryDruidTableDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTableDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidTableDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidTableDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (table). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidInlineDataSource'
        if ($JsonData.PSobject.Properties["type"].value -eq "telemetry.DruidInlineDataSource") {
            # try to match TelemetryDruidInlineDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidInlineDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidInlineDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidInlineDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (telemetry.DruidInlineDataSource). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidJoinDataSource'
        if ($JsonData.PSobject.Properties["type"].value -eq "telemetry.DruidJoinDataSource") {
            # try to match TelemetryDruidJoinDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidJoinDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidJoinDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidJoinDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (telemetry.DruidJoinDataSource). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidLookupDataSource'
        if ($JsonData.PSobject.Properties["type"].value -eq "telemetry.DruidLookupDataSource") {
            # try to match TelemetryDruidLookupDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidLookupDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidLookupDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidLookupDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (telemetry.DruidLookupDataSource). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidQueryDataSource'
        if ($JsonData.PSobject.Properties["type"].value -eq "telemetry.DruidQueryDataSource") {
            # try to match TelemetryDruidQueryDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidQueryDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidQueryDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidQueryDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (telemetry.DruidQueryDataSource). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidTableDataSource'
        if ($JsonData.PSobject.Properties["type"].value -eq "telemetry.DruidTableDataSource") {
            # try to match TelemetryDruidTableDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTableDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidTableDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidTableDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (telemetry.DruidTableDataSource). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'telemetry.DruidUnionDataSource'
        if ($JsonData.PSobject.Properties["type"].value -eq "telemetry.DruidUnionDataSource") {
            # try to match TelemetryDruidUnionDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidUnionDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidUnionDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidUnionDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (telemetry.DruidUnionDataSource). Proceeding with the typical oneOf type matching."
            }
        }

        # check if the discriminator value is 'union'
        if ($JsonData.PSobject.Properties["type"].value -eq "union") {
            # try to match TelemetryDruidUnionDataSource defined in the oneOf schemas
            try {
                $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidUnionDataSource $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "TelemetryDruidUnionDataSource"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "oneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in oneOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'TelemetryDruidUnionDataSource' defined in oneOf (IntersightTelemetryDruidDataSource) using the discriminator lookup (union). Proceeding with the typical oneOf type matching."
            }
        }

        # try to match TelemetryDruidInlineDataSource defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidInlineDataSource $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidInlineDataSource"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidInlineDataSource' defined in oneOf (IntersightTelemetryDruidDataSource). Proceeding to the next one if any."
        }

        # try to match TelemetryDruidJoinDataSource defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidJoinDataSource $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidJoinDataSource"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidJoinDataSource' defined in oneOf (IntersightTelemetryDruidDataSource). Proceeding to the next one if any."
        }

        # try to match TelemetryDruidLookupDataSource defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidLookupDataSource $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidLookupDataSource"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidLookupDataSource' defined in oneOf (IntersightTelemetryDruidDataSource). Proceeding to the next one if any."
        }

        # try to match TelemetryDruidQueryDataSource defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidQueryDataSource $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidQueryDataSource"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidQueryDataSource' defined in oneOf (IntersightTelemetryDruidDataSource). Proceeding to the next one if any."
        }

        # try to match TelemetryDruidTableDataSource defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidTableDataSource $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidTableDataSource"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidTableDataSource' defined in oneOf (IntersightTelemetryDruidDataSource). Proceeding to the next one if any."
        }

        # try to match TelemetryDruidUnionDataSource defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-IntersightJsonToTelemetryDruidUnionDataSource $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "TelemetryDruidUnionDataSource"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'TelemetryDruidUnionDataSource' defined in oneOf (IntersightTelemetryDruidDataSource). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([TelemetryDruidInlineDataSource, TelemetryDruidJoinDataSource, TelemetryDruidLookupDataSource, TelemetryDruidQueryDataSource, TelemetryDruidTableDataSource, TelemetryDruidUnionDataSource]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("TelemetryDruidInlineDataSource", "TelemetryDruidJoinDataSource", "TelemetryDruidLookupDataSource", "TelemetryDruidQueryDataSource", "TelemetryDruidTableDataSource", "TelemetryDruidUnionDataSource")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([TelemetryDruidInlineDataSource, TelemetryDruidJoinDataSource, TelemetryDruidLookupDataSource, TelemetryDruidQueryDataSource, TelemetryDruidTableDataSource, TelemetryDruidUnionDataSource]). JSON Payload: $($Json)"
        }
    }
}


